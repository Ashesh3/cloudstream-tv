rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Cloudframe has no Firebase browser SDK. Every request is authorized by
    // the same-origin Vercel API using server IAM.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
